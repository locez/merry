{
  "name": "@merry/web",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:trajectory-contract": "node scripts/generate-trajectory-contract.mjs",
    "check:trajectory-contract": "node scripts/generate-trajectory-contract.mjs --check",
    "build": "npm run generate:trajectory-contract && tsc --project tsconfig.json && cp dist/index.js dist/app.js && cp index.html dist/index.html && cp src/app.css dist/app.css",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
